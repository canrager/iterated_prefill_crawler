# Model selection ("local" or openrouter path)
target_model: "x-ai/grok-4.1-fast"
translation_model: "allenai/olmo-3.1-32b-instruct"
summarization_model: "allenai/olmo-3.1-32b-instruct"
refusal_check_model: "allenai/olmo-3.1-32b-instruct"

# Local model config — set to null for API-only (no GPU) runs.
# Auxiliary roles above are routed to OpenRouter instead.
local_model: null
quantization_bits: null
device: "cpu"
cache_dir: "hf_models"
vllm_tensor_parallel_size: 1
vllm_gpu_memory_utilization: 0.8
vllm_max_model_len: 1000
